spring:
  application:
    name: monolith-api
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: pass
  jpa:
    hibernate:
        ddl-auto: update
    open-in-view: true
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /docs